Our official repository (from the professor):
https://github.com/UTSA-CS-3443/gowiththeflow.git


Coding Standards:

Everything:
	-class names should be short and describe their primary function (what the output is)
	-lines cannot be more than 100 characters long
	-variable names should describe what they represent
	-always press CTRL-Shift-O and CTRL-Shift-F before closing a .java file

Comments:
	-there should be a space immediately after every //
	-comments that span more than one line should use the multi-line comment tags

Javadoc:
	-all javadoc class comments must include all group members as the authors